services:
  fetch:
    image: mcp/fetch
    container_name: fetch-mcp
    stdin_open: true
    tty: true
    restart: unless-stopped
    networks:
      - mcp-network

networks:
  mcp-network:
    driver: bridge
